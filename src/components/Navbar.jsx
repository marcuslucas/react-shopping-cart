import React from "react";

const Navbar = (props) => {
  return (
    <div>
      <div>Logo</div>
      <div>
        <button>Home</button>
        <button>Cart: __item__</button>
      </div>
    </div>
  );
};

export default Navbar;
